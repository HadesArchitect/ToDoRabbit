# ToDoRabbit Backend

FastAPI-based backend for the ToDoRabbit application.

## Setup

```bash
# Install dependencies
pip install -e ".[dev]"

# Run tests
pytest

# Run development server
uvicorn app.main:app --reload
```
